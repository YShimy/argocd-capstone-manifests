apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: capstone-app
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: capstone-app-stable
                port:
                  number: 80
